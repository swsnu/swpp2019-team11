matrix:
  include:
  - language: node_js
    node_js:
    - '10'
    before_install:
    - cd front
    install:
    - yarn install
    - npm install
    script:
    - ./node_modules/.bin/eslint src/
    - yarn test --coverage --watchAll=false
  - language: python
    python:
    - '3.6'
    before_install:
    - cd ml
    install:
    - pip3 install django
    script:
    - pylint **/*.py